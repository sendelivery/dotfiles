# dotfiles

## Usage

On a new machine, run the following command to install the dotfiles and tools:

```bash
curl -sL https://raw.githubusercontent.com/sendelivery/dotfiles/main/install.sh | bash
```
